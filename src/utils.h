#ifndef _UTILS_H
#define _UTILS_H

/**
 * Display information
 *
 * @param fmt Format
 */
void info(const char *fmt, ...);

/**
 * Display errors
 *
 * @param fmt Format
 */
void err(const char *fmt, ...);

#endif
